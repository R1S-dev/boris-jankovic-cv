# Boris Janković — CV (React + Tailwind)

## Run
```bash
npm install
npm run dev
```

## Notes
- No backend. GitHub projects are loaded live from public GitHub API.
- Includes: command palette (Ctrl+K), skills matrix, case-study cards, lightweight animations.
- Edit personal info in `src/data/profile.js`.
